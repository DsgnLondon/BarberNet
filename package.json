{
  "name": "workspace-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix Newfolder run start",
    "android": "npm --prefix Newfolder run android",
    "ios": "npm --prefix Newfolder run ios",
    "web": "npm --prefix Newfolder run web",
    "lint": "npm --prefix Newfolder run lint",
    "reset-project": "npm --prefix Newfolder run reset-project"
  },
  "dependencies": {
    "eas-cli": "^16.26.0",
    "expo": "^54.0.20"
  },
  "devDependencies": {
    "@expo/ngrok": "^4.1.3",
    "@types/react": "~19.1.10",
    "expo-module-scripts": "^5.0.7"
  }
}
